PACKAGE_NAME=com.mrdula.streamdeck
MENTRAOS_API_KEY=your_api_key_here
PORT=3000
# Optional: max managed-stream length in seconds (default 14400 = 4h). Omitting this in-app used to hit a ~1m cloud default.
# STREAM_MAX_DURATION_SECONDS=14400
